rules_version = '2';

// Firebase Storage 보안 규칙
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 인증된 사용자는 읽기/쓰기 허용
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
